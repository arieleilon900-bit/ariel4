# יצירת קשר / CTA

## ממצא מרכזי: אף אתר שבנינו לא משתמש ב-`<form>`

בדקנו את כל 12 האתרים הקיימים (`clients/*`) — אף אחד לא כולל טופס יצירת קשר אמיתי (`<form>`, `<input>`, שליחה לשרת/מייל). כולם משתמשים אך ורק בקישורי פעולה ישירים:

- `tel:+972XXXXXXXXX` — שיחה ישירה
- `https://wa.me/972XXXXXXXXX` — הודעת וואטסאפ

**זו בחירה מכוונת, לא פספוס.** לעסקים מקומיים קטנים (מנעולן, אינסטלטור, מכבסה) הלקוח רוצה מענה מיידי — טופס עם "נחזור אליכם" מוריד המרה. תישארו עם התבנית הזו כברירת מחדל.

## תבנית סטנדרטית

כפתור טלפון ראשי + כפתור וואטסאפ משני, חוזרים בלפחות 3 מקומות בעמוד: ה-Hero, סקציית "יצירת קשר"/פרטי התקשרות בתחתית, ופס CTA צף/sticky בתחתית המסך (ראו `clients/moshayev-lock` שורה 599).

```html
<a href="tel:+972XXXXXXXXX" class="btn btn-primary">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><!-- אייקון טלפון --></svg>
  התקשרו עכשיו
</a>
<a href="https://wa.me/972XXXXXXXXX" target="_blank" rel="noopener" class="btn btn-outline-light">
  <svg viewBox="0 0 24 24" fill="currentColor"><!-- אייקון וואטסאפ --></svg>
  שלחו הודעה בוואטסאפ
</a>
```

מספר הטלפון בפורמט `wa.me` תמיד ללא `+` וללא רווחים/מקפים: `972522299868`. במספר ה-`tel:` כן שומרים `+`.

## סקציית פרטי קשר בתחתית

תבנית שחוזרת (`moshayev-lock` שורות ~570-590): כרטיס עם `label` ("התקשרו") ו-`value` שהוא לינק `tel:`, ולידו שוב שני הכפתורים הראשיים. חשוב שהמספר עצמו יהיה קליקבילי, לא רק הכפתור.

## מתי כן להוסיף טופס
אם לקוח עתידי מבקש טופס אמיתי (לצורך תיעוד לידים ב-CRM, לא רק שיחה מיידית) — אפשר, אבל:
- אל תחליפו את כפתורי `tel:`/`wa.me`, רק תוסיפו טופס כאופציה שלישית
- אל תבנו backend חדש סתם — Formspree / Google Forms embed / mailto: מספיקים לרוב הלקוחות האלה
- תעדו כאן תבנית חדשה אם זה עובד טוב, כדי שהפעם הבאה לא נמציא מחדש
